namespace m4dModels
{
    // DanceQueryNext is now merged into DanceQuery.
    // Retain as a stub for future breaking changes.
    public class DanceQueryNext(string query = null) : DanceQuery(query)
    {
        // Stub for future use.
    }
}
