@using m4d.ViewModels
@model m4dModels.Search

@{
    ViewBag.Title = "Remove from History";

ViewData["BreadCrumbs"] = new List<BreadCrumbItem>
{
    BreadCrumbItem.HomeItem,
    new()
    {
        Title = Model.ApplicationUser.UserName,
        Link = "/Identity/Account/Manage"
    },
    new()
    {
        Title = "Searches",
        Link = "/Searches",
    },
    new()
    {
        Title = ViewBag.Title,
        Active = true
    }
};

var sort = ViewBag.Sort;
var showDetails = ViewBag.ShowDetails;
var spotifyOnly = ViewBag.SpotifyOnly;
var user = ViewBag.User;
}


<h1>@ViewBag.Title</h1>
<hr/>

<h3>Remove this search from your history?</h3>
<p class="text-muted">The search data will be kept for site-wide statistics.</p>
<div>
    <p>@Model.Filter.Description</p>

    @using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions no-color">
            <input type="submit" value="Remove from History" class="btn btn-danger"/> |
            @Html.ActionLink("Back to Search", "Index", new { sort, showDetails, spotifyOnly, user})
        </div>
}
</div>
