@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    // It looks like this is handled correctly by the bunding mechanism, leaving this in so I 
    //  have a reference it IsDebuggingEnabled
    //if (HttpContext.Current.IsDebuggingEnabled)
    //{
    //    Page.ScriptQualifier = "?" + Html.Raw(DateTime.Now.Ticks.ToString());
    //}
    //else
    //{
    //    // TODO: Figure out how to autoincrement
    //    Page.ScriptQualifier = "?3";
    //}
}
