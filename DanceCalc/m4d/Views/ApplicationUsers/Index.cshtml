@using Microsoft.AspNet.Identity
@model ApplicationUserManager

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
*@
<table class="table">
    <tr>
        @*
    <th>
        @Html.DisplayNameFor(model => model.PasswordHash)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.SecurityStamp)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.PhoneNumber)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.PhoneNumberConfirmed)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.TwoFactorEnabled)
    </th>
        *@
        <th>
            LOS
        </th>
        <th>
            UserName
        </th>
        <th>
            Email
        </th>
        <th>
            Date
        </th>
        <th>
            Roles
        </th>
        <th>
            Logins
        </th>
        <th>
            EMC
        </th>
        <th>
            Lock-End
        </th>
        <th>
            AFC
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Users.OrderByDescending(u => u.StartDate).ToList())
{
    <tr>
        @*
        <td>
            @Html.DisplayFor(modelItem => item.PasswordHash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecurityStamp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumberConfirmed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TwoFactorEnabled)
        </td>
            *@
        <td>
            @Html.DisplayFor(modelItem => item.LockoutEnabled)
        </td>
        <td>
            @Html.ActionLink(item.UserName, "Details", new { id = item.Id })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @item.StartDate.ToString("d")
        </td>
        <td>
            @string.Join(",",Model.GetRoles(item.Id))
        </td>
        <td>
            @string.Join(",",Model.GetLogins(item.Id))
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailConfirmed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LockoutEndDateUtc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccessFailedCount)
        </td>
        <td>
            @Html.ActionLink("Change Roles", "ChangeRoles", new { id = item.Id }),
            @Html.ActionLink("List Songs", "FilterUser", "Song", new { user = item.UserName }, null),
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            @*
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            *@
        </td>
    </tr>
}
</table>

@Html.ActionLink("New Pseudo User", "Create", null, new { @class = "btn btn-default", role = "button" })
